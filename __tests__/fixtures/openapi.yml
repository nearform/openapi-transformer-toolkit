openapi: 3.0.0
info:
  title: Test API
  version: 1.0.0
paths: {}
components:
  schemas:
    Example:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        surname:
          type: string
      required: [id, name, surname]
