export const headers_X_Expires_After = {
  description: "date in UTC when token expires",
  schema: { type: "string", format: "date-time" },
  title: "X-Expires-After",
  $id: "headers_X_Expires_After.json",
} as const;
